# Docker completions, generated on first use by the docker CLI itself.
# Always matches the installed docker version and active plugins (compose, buildx, …).
# Replaces the old static dump from the dead barnybug/docker-fish-completion project.
command -q docker; and docker completion fish | source
